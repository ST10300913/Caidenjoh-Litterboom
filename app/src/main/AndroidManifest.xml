<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LitterBoom"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDO58-xeFOVUtdHSEQbuNr0HtwvPvBdUZM" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.BagEntryActivity" />

        <activity
            android:name=".WasteWorkerActivity"
            android:exported="false"
            android:label="User Dashboard" >
        </activity>

        <activity
            android:name=".ui.MainLoggingMenuActivity"
            android:exported="false"
            android:label="Logging Main Menu" >
        </activity>

        <activity
            android:name=".ui.FieldLoggingActivity"
            android:exported="false"
            android:label="Log Item Details" >
        </activity>

        <activity
            android:name=".ui.EventSelectionActivity"
            android:exported="false"
            android:label="Select Event" />

    </application>

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Needed only on Android 9 and below -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />


</manifest>